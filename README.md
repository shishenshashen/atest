# atest

Smoke-test repo used to verify Hermes Agent's GitHub integration on 2026-06-09.

- Token: configured via `.env` (GITHUB_TOKEN)
- Auth: `git-credential-manager` (Windows Credential Store)
- Identity: `OrientWan <shishenshashen@users.noreply.github.com>`

If you can see this commit, the full clone → commit → push loop works end-to-end.
