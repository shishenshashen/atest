# 2026-06-04 10:00 巡检 — 详细任务规划

> 触发：`自动巡检` cron · session `mvs_b505fc7ac43e41d7bf9d28d38289969e`
> 队列入口：`00-任务调度中心/队列.md`（顶部 4 个 track 索引）
> owner：Mavis（编排+督促）· executor：mavis-team workers

---

## 现状摘要（10:00 实扫）

| 维度 | 值 |
| --- | --- |
| `队列.md` 任务数 | 14 条全部 done（plan_bbbdc7f5 一波 8/8 PASS） |
| `归档.md` 条目 | 8+ 条 done（2026-06-04） |
| 活跃 plan | 0（`mavis team plan status` 空） |
| 卡死 session | 0（无 zombie / 无 inflight worker） |
| MT5 进程 | terminal64 PID 7540 + MetaEditor64 PID 4048 都在跑 |
| CU keep-alive | 3 实例在跑（PID 4260 / 6936 / 2684） |
| **可分派工作** | **4 类（队列已空，只能收尾+规划）** |

---

## Track 1：daily/ 临时报告归档（14 个 mql5kit diff/verify 文件）

**目的**：收尾 06-04 跑 mql5kit 3-way fork 统一时产生的临时 diff/verify 报告，daily/ 不留散件。

**scope**：
```
daily/diff-mql5kit-2026-06-04_01-05-17.{md,stats.json,sync-plan.json}        → 3 文件
daily/diff-mql5kit-2026-06-04_01-35-04.{md,stats.json}                       → 2 文件
daily/diff-mql5kit-2026-06-04_01-35-51.{md,stats.json}                       → 2 文件
daily/diff-mql5kit-2026-06-04_01-36-27.{md,stats.json}                       → 2 文件
daily/fork-c-verify-2026-06-04_01-15-57.{md,stats.json}                      → 2 文件
daily/fork-c-verify-2026-06-04_01-20-03.{md,stats.json}                      → 2 文件
daily/cleanup-3rd-fork-2026-06-04_09-25-02.md                                → 1 文件
合计 14 文件
```

**执行步骤**（worker 严格按序）：
1. `cd "C:\ai\obsidian-文件\mt\00-任务调度中心\daily"`
2. 每个 diff 组（4 组共 9 文件）→ **保留 md 报告** + **删除冗余 stats.json**（md 内已嵌入统计）
   - 保留：`*.md`（人类可读报告）
   - 删除：`*.stats.json`（机器统计，md 内已含）
   - 例外：`diff-mql5kit-2026-06-04_01-05-17.sync-plan.json` **保留**（mavis-team 后续可能需要）
3. `fork-c-verify-*`（2 组 4 文件）→ **删除全部**（verify 是中间产物，最终结论已在 `cleanup-3rd-fork-2026-06-04_09-25-02.md` 里）
4. `cleanup-3rd-fork-2026-06-04_09-25-02.md` **保留**（终态报告）
5. 清理后 daily/ 应剩 5 文件：`2026-06-03.md` / `2026-06-04.md` / `2026-06-04_10-00-plan.md`（本文件）/ `cleanup-3rd-fork-...md` / `diff-mql5kit-...sync-plan.json` / `README.md`

**deliverable**：
- 5 文件清单写入本文件末"Track 1 完成态"
- 删除操作前**先 dry-run**（`Get-ChildItem` 列出待删文件清单贴到 deliverable），确认无误再删

**verifier**（worker 自己二次核对）：
- `Get-ChildItem` 列出清理后 daily/ 文件清单
- 确认 5 文件全在 + 14 文件无残留
- 检查无 `*.stats.json` 残留（除了 03-03.md / 03-04.md / sync-plan.json）

**预估**：15-20 分钟（纯文件 I/O + 校验）

**反模式禁止**：
- ❌ 删 03-04.md（当日日志主体，68KB）
- ❌ 删 README.md（daily 目录说明）
- ❌ 用 `rm -rf`（用 mavis-trash，参见 powershell-windows 记忆）
- ❌ 删 sync-plan.json（mavis-team 复用）

---

## Track 2：3 个长期 parked 项目状态 review

**目的**：评审 3 个长期搁置项，决定保留/重启/取消。

**scope**（输入）：`任务.md` 末段 3 个 `[parked]`
1. **MT5 GUI stub 9 个**（focus / open_strategy_tester / select_ea / ...）
   - 卡点：需要 console session 1（CU 截屏在 RDP 失败）
   - 解决：已加 `mt5-gui-driver` skill + `mcp-mt5-v2` 27 tools，**实际是不是已经能跑了？** → 需 review
2. **asar 根除 CU auto-disable**
   - 卡点：asar 是渲染器内置代码，改不动
   - 解决：5s keep-alive 兜底，已在跑
   - 决策候选：保留 parked / 标 done（兜底方案已落地）
3. **调度中心根目录 3 份 worker 报告待归档**
   - 卡点：等用户授权
   - 解决：本次 Track 2.3 一并 review

**执行步骤**（worker）：
1. **2.1 MT5 GUI stub review**：
   - `mavis mcp call mt5 mt5_focus '{}'` 实测能不能跑（用真实 MT5 终端 PID 7540）
   - 如果能跑 → 标 done，写明"Phase 1 时 GUI 受限已解，9 个 stub 实测可用"
   - 如果不能跑 → 写明卡点（什么 session/什么错误），建议方案
   - 9 个 tool 全部测一遍，列测试矩阵
2. **2.2 asar 根除 review**：
   - 检查 keep-alive 自 23:00 至今的 auto-disable 事件数（应为 0）
   - 翻 keep-alive 日志最后 100 行
   - 决策：标 done（兜底稳）or 保留 parked
3. **2.3 调度中心根目录 worker 报告归档**：
   - `ls "C:\ai\obsidian-文件\mt\"` 找 3 份 worker 报告
   - 内容提取 → 写入 `归档.md` 3 条 done
   - 物理移动到 `daily/` 或 `_archive/`（user 决定路径）
   - 决策：本次直接做（不再 parked）— 因为内容已经审计

**deliverable**：
- `任务.md` 末段 3 个 [parked] 状态更新（保留/转 done/转 redo）
- `归档.md` 新增 2-3 条 done（GUI stub 实测 + 报告归档）
- 一份 `daily/2026-06-04_10-00-parked-review.md` 总结（2.1 测试矩阵 + 2.2 keep-alive 证据 + 2.3 报告归档映射）

**verifier**：
- 9 个 tool 测试矩阵完整（每个一行：tool 名 / 调用 / 返回 / 结论）
- keep-alive 日志引用行号
- 归档前/后文件 diff 列表

**预估**：30-40 分钟（GUI stub 实测 20 分钟 + 报告归档 10 分钟 + keep-alive review 5 分钟）

---

## Track 3：下一波任务规划（队列空了，要补下一波）

**目的**：队列清空后，写一份 1-2 周内可执行的下波任务清单进 Obsidian 队列。

**scope**（输入）：MT5 EA 知识库 + 现有 5 个 minimax-ea + MQL5Kit 18 模块 + _archive/ 244 文件

**执行步骤**（worker）：
1. **盘点现有资产**：
   - `Get-ChildItem "C:\ai\obsidian-文件\mt\MQL5\Experts\minimax-ea\"` → 5 个 EA
   - `Get-ChildItem "C:\ai\obsidian-文件\mt\MQL5\Include\MQL5Kit\"` → 18 个模块
   - `Get-ChildItem "C:\ai\obsidian-文件\mt\MQL5\Experts\_archive\"` → 219 文件
   - 翻 MT5 知识库 `C:\ai\obsidian-文件\mt\` 找"待办" / "TODO" / "P?"
2. **生成候选任务**（每条预估工时+依赖+阻塞点）：
   - **候选 A：5 个 EA 实盘/回测对比**（候选 EA: TrendMA / DonchianXAU / MeanRev / RSI / MA_Cross / Grid）
     - 任务：每个 EA 跑 1 次 6 月回测，生成对比表
     - 预估：2h/EA × 6 = 12h
     - 阻塞：console session 1（GUI 编译）✅
   - **候选 B：MQL5Kit 补 5 个新模块**（候选: M11 RiskManager / M12 PositionSizer / M14 TradeLogger / M16 SessionFilter / M18 CorrelationFilter）
     - 任务：每个新模块实现 + 单元测试 + 1 个 EA 接入 demo
     - 预估：3h/模块 × 5 = 15h
     - 阻塞：无
   - **候选 C：_archive/ 219 文件审计**（挑可复活 EA 进 minimax-ea/）
     - 任务：分类（草稿/Test*/可复活）+ 复活 2-3 个到正式目录
     - 预估：8h
     - 阻塞：console session 1
   - **候选 D：知识库续写**（minimaxcode 知识库 + MT5 知识库）
     - 任务：每个新模块/EA 写 1 篇 wiki（含代码示例 + 编译步骤 + 接入 demo）
     - 预估：1h/篇 × 10 = 10h
     - 阻塞：无
3. **优先级排序 + 写入队列**：
   - top 5 任务写入 `队列.md`（顶部，按 P0/P1/P2）
   - 每条格式同现有 `- [ ] <任务> (依据：... | 预估：Xh) → **<日期> [queued]**`

**deliverable**：
- `队列.md` 顶部插入 5 条新任务（替换现有 14 条 done 或在 done 上方）
- `daily/2026-06-04_10-00-next-wave.md` 详细规划（候选 A-D 完整分析 + 5 条 top 选型理由）

**verifier**：
- 5 条任务每条都有"依据 | 预估 | 阻塞"三段
- 候选 A-D 完整覆盖（不能漏）
- 与现有 14 条 done 不重复

**预估**：60-90 分钟（资产盘点 20 分钟 + 候选生成 30 分钟 + 写入 + 校验 20 分钟）

---

## Track 4：卡点扫描（确认无僵尸 session / plan / worker）

**目的**：扫一遍活跃进程 + 锁 + 网络，确认无卡死/卡点。

**scope**：
- `mavis session list --status started,inflight`（活跃 session）
- `mavis team plan status`（活跃 plan）
- `mavis communication list --status pending`（未读消息）
- `Get-Process | Where-Object { $_.Name -match 'terminal64|MetaEditor' }`（MT5 进程）
- `Get-ChildItem "C:\Users\Administrator\.mavis\plans\" -Recurse | Where-Object { $_.LastWriteTime -gt (Get-Date).AddHours(-2) }`（最近 2h plan 目录）
- 锁文件扫描：`Get-ChildItem "C:\Users\Administrator\.mavis\plans\*\*.lock" -ErrorAction SilentlyContinue`

**执行步骤**（worker）：
1. 跑上面 6 条命令
2. 输出"卡点扫描报告"到 `daily/2026-06-04_10-00-stuck-scan.md`
3. 如果发现：
   - 僵尸 session（started 状态 >2h 无响应）→ 列 PID + session_id + 卡点
   - 活跃 plan 长时间无 progress → 列 plan_id + cycle 数 + 阻塞
   - 未读 communication → 列 from/to/content
   - MT5 进程死锁 → PID + 症状
4. 全部健康 → 写"✅ 无卡点" + 关键证据

**deliverable**：
- `daily/2026-06-04_10-00-stuck-scan.md` 报告
- 关键证据（命令输出）

**verifier**：
- 6 条命令全部跑过（不漏）
- 报告引用具体 session_id / plan_id / PID

**预估**：10-15 分钟

---

## 分派矩阵

| Track | 主题 | executor | 预估 | 阻塞 |
| --- | --- | --- | --- | --- |
| 1 | daily/ 临时报告归档 | worker-A | 15-20 min | 无（纯文件） |
| 2 | 3 parked 项目 review | worker-B | 30-40 min | GUI 测需 console session 1 |
| 3 | 下一波任务规划 | worker-C | 60-90 min | 无（研究类） |
| 4 | 卡点扫描 | worker-D | 10-15 min | 无 |

**总预估**：max(track 3) = 90 分钟（含并行）

**owner 角色**（Mavis 本 session）：
- ✅ 启动 mavis-team plan
- ✅ 收 CycleReport 后决策 accept / retry
- ✅ 写本时段日志到 daily/2026-06-04.md
- ✅ 不直接做 Track 1-4 工作（按用户 IM："你只做规划和督促，让其他的做任务"）

---

## 反模式提醒（worker 必看）

- ❌ **不要重启 terminal64 / MetaEditor64**（用户可能正在用，碰了 GUI 编译窗口会丢焦点）
- ❌ **不要删 .ex5 文件**（已编译产物，重编译耗 GUI session）
- ❌ **不要碰 `00-任务调度中心/` 根目录 3 份 worker 报告**（Track 2.3 专属）
- ❌ **不要跨 sandbox 路径**（用户 MT5 在 `AppData\Roaming\MetaQuotes\Terminal\<hash>\MQL5\`，不是 `C:\Program Files\`）
- ❌ **不要在 RDP session 操作 GUI**（desktop_screenshot 会失败）
- ✅ **所有写入先 dry-run**（先列出待操作清单，确认后再做）
- ✅ **每个 track 完成后写 deliverable 到对应 daily/*.md 路径**（mavis-team 验收只看这个）
